#include <stdio.h>

/**
* main - Entry point
* Description: writes a string to stdout with the puts function
* Return: Always 0 (Success)
*/
int main(void)
{
	puts("Programming is like building a multilingual puzzle");
	return (0);
}
